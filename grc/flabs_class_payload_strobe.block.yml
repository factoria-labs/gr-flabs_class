id: flabs_class_payload_strobe
label: Payload Strobe
category: '[Factoria Labs Blocks]'

templates:
  imports: from gnuradio import flabs_class
  make: flabs_class.payload_strobe(${payloads}, ${tx_spacing}, ${repeat_count}, ${packet_ctr_enable})

#  Make one 'parameters' list entry for every parameter you want settable from the GUI.
#     Keys include:
#     * id (makes the value accessible as keyname, e.g. in the make entry)
#     * label (label shown in the GUI)
#     * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#     * default
parameters:
- id: payloads
  label: Payloads
  dtype: ''
  default: ((0x12, 0x34, 0x56, 0x78, 0x9a), (0xbc, 0xde, 0xf0))
- id: tx_spacing
  label: Period (seconds)
  dtype: float
  default: 1
- id: repeat_count
  label: Repeat Count
  dtype: int
  default: 0
- id: packet_ctr_enable
  label: Packet Counter
  dtype: enum
  default: '0'
  options: ['0', '1', '2']
  option_labels: ['Disabled', 'Binary Counter', 'ASCII Counter']

#  Make one 'inputs' list entry per input and one 'outputs' list entry per output.
#  Keys include:
#      * label (an identifier for the GUI)
#      * domain (optional - stream or message. Default is stream)
#      * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#      * vlen (optional - data stream vector length. Default is 1)
#      * optional (optional - set to 1 for optional inputs. Default is 0)
outputs:
-   domain: message
    id: out

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
